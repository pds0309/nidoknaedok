<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.config.MemberMapper">
    <insert id="signUpMember" parameterType="com.dto.member.MemberDTO$SignUp">
        insert into members (name, password, email, address, address_detail, authority, point)
        values (#{name}, #{password}, #{email}, #{address}, #{addressDetail}, #{authority}, 100)
    </insert>

    <select id="findByName" parameterType="string" resultType="com.dto.member.MemberDTO$Info">
        select id, name, to_char(created_at,'YYYY/MM/DD') as created_at
        from members
        where name = #{name}
    </select>

    <select id="findByEmail" parameterType="string" resultType="com.dto.member.MemberDTO$Info">
        select id, name, to_char(created_at,'YYYY/MM/DD') as created_at
        from members
        where email = #{email}
    </select>

</mapper>   